/**
 * @File Testing out things because typescript is amazing
 * 	and insanely difficult to read if you start to get complicated
 *
 * @Author Alexander Bassov Wed Aug 03 2022
 * @Email blackxes.dev@gmail.com
 */

// import { AppState } from "./Redux/types";

// type CustomKeyAppState = Extract<keyof AppState, String>;
// type WithoutCombined = {
//     [K in CustomKeyAppState]: AppState[K];
// };
// type Values<T> = T[keyof T];

// type DemoValues = Values<WithoutCombined>;

// type ColorMap<T extends Record<string, Record<string, unknown>>> = {
//     [Prop in keyof T]: keyof T[Prop];
// };

// type Result = Values<ColorMap<WithoutCombined>>;
